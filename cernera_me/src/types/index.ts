export type GitHubRepository = {
    id: number,
    name: string,
    description: string,
    html_url: string
}

export type TechLogo = {
    image: string,
    name: string,
    description?: string
}